# RAG Hybrid Search

Local Java 21/Spring Boot RAG with BGE-M3 dense+sparse embeddings, Qdrant, Kafka, PostgreSQL, and Qwen3 via Ollama. Place manually downloaded ONNX models and tokenizers in `models/`, start Docker dependencies, then run `gradle bootRun`.
